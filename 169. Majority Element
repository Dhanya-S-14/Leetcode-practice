169. Majority Element

class Solution {
    public int majorityElement(int[] nums) {
        int count=0,maj=0;
   for(int num:nums){
 if(count==0){
    maj=num;
    count=1;
 }else if(maj==num){
    count++;
 }else{
   count--;
 }
   }
   return maj;
    }
}
    
